# frozen_string_literal: true

module <%= camelcase(name) %>
  # A single container for mixins that are shared among plots, chapters, and
  # subplots.
  #
  module Common
    extend Gamefic::Scriptable

    include Gamefic::Standard
  end
end
